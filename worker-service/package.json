{
  "name": "booking-worker-service",
  "version": "1.0.0",
  "description": "Worker service for processing booking jobs from all queue types",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "bullmq": "^5.3.0",
    "ioredis": "^5.3.2",
    "kafkajs": "^2.2.4",
    "dotenv": "^16.3.1",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/winston": "^2.4.4",
    "nodemon": "^3.0.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}

